// Func to reverse a String

function reverseString(str) {
    // 1. split() to return as new array
    // 2. reverse() the new created array
    // 3. join() join all into a string
   return str.split('').reverse().join('');
}